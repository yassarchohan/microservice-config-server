zuul:
  prefix: /api
  ignoredServices: '*'
  routes:
    api-gateway: /gateway/**
    auth: /auth/**
    ecare-appointment-service: /ecare/**

server:
  port: 8080
  compression:
    enabled: true
    mime-types: application/json,text/css,application/javascript
    min-response-size: 2048
